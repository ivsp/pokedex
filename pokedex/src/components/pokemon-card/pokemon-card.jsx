import { useEffect } from "react";

function PokemonCard() {
  /**
   * Tengo que crear un useEfect para realizar el fetch a la API
   * cuando tengo los datos, los guardo en una variable de estado
   * que almacenará todos los valores de los pokemon.
   * Antes de guardar la información debo ordenar los pokemon por ID
   * función sort del array
   *
   */

  useEffect(() => {}, []);

  return <h1>POKEMON CARD</h1>;
}

export default PokemonCard;
